# Strapi REST base URL (no trailing slash). Required for layout data and page blocks.
# Example local: http://localhost:1337
PUBLIC_STRAPI_URL=

# Set to true to relax prerender cache while building previews (optional).
# PUBLIC_PREVIEW_MODE=
